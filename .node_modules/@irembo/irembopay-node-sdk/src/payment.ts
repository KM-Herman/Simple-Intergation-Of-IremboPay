import { MobileMoney } from "./mobileMoney";
import { Base } from "./base";

export class Payment extends Base {
  public mobileMoney: MobileMoney;
  constructor(apiKey: string, environment: Environment) {
    super(apiKey, environment);
    this.mobileMoney = new MobileMoney(this.axiosInstance);
  }
}
