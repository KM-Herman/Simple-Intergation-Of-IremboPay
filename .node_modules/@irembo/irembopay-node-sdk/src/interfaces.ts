interface Customer {
  email: string;
  phoneNumber: number;
  name: string;
}
interface PaymentItem {
  code: string;
  quantity: number;
  unitAmount: number;
}
interface CreateInvoiceInterface {
  transactionId: number;
  paymentItems: PaymentItem[];
  paymentAccountIdentifier: number;
  expiryAt: Date;
  description: string;
  customer: Customer;
  language: string;
}

interface CreateBatchInvoiceInterface {
  transactionId: string;
  invoiceNumbers: number[];
  description: string;
}

interface UpdateInvoiceInterface {
  expiryAt: Date;
  paymentItems: PaymentItem[];
}

type Environment = "production" | "sandbox" | "checkout";

interface MobileMoneyInterface {
  accountIdentifier: string;
  paymentProvider: string;
  invoiceNumber: string;
  transactionReference: string;
}
